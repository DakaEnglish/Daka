<!--pages/select/select.wxml-->

<view wx:for="{{teacherList}}">
  <image src="{{item.imag}}"></image>
  <view>{{item.name}}</view>
  <view>{{item.info}}</view>
  <button bindtap="gotoPage" data-num="{{index}}">查看</button>
</view>